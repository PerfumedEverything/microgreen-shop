# 📘 ПАМЯТКА: Реализация каталога в Next.js

## 📍 Расположение файлов
```
app/
├── sections/
│   └── catalog.tsx          # Секция на главной (3 товара)
├── catalog/
│   └── page.tsx             # Полная страница каталога
└── CATALOG_GUIDE.md         # Эта памятка
```

---

## 🎯 Архитектура каталога

### 1. ДВУУРОВНЕВАЯ СТРУКТУРА

**Уровень 1: Главная страница** (`app/sections/catalog.tsx`)
- ✅ Только 3 популярных товара (бестселлеры)
- ✅ Быстрый просмотр
- ✅ Кнопка "Перейти в каталог" → `/catalog`
- ✅ Без фильтров и сложной логики

**Уровень 2: Страница каталога** (`app/catalog/page.tsx`)
- ✅ Все товары (12+)
- ✅ Полный функционал фильтрации
- ✅ Поиск по названию и описанию
- ✅ Сортировка (5 вариантов)
- ✅ Два режима отображения (Grid/List)
- ✅ Фильтры по категориям, цене, особым меткам

---

## 🧩 Функционал каталога

### 🔍 Поиск
```typescript
// Реализация поиска
const filteredProducts = useMemo(() => {
  if (searchQuery) {
    const query = searchQuery.toLowerCase()
    result = result.filter(p => 
      p.name.toLowerCase().includes(query) || 
      p.description.toLowerCase().includes(query)
    )
  }
}, [searchQuery, ...])
```
**Особенности:**
- Поиск по названию И описанию
- Немедленная фильтрация (без debounce для простоты)
- Кнопка очистки поиска
- Empty state при отсутствии результатов

### 🏷️ Фильтры

#### 1. По категориям
```typescript
const categories = [
  { id: "all", name: "Все", count: allProducts.length },
  { id: "vegetables", name: "Овощи", count: ... },
  { id: "herbs", name: "Зелень", count: ... },
  // ...
]
```

#### 2. По цене (диапазон)
```typescript
const [priceRange, setPriceRange] = useState({ min: "", max: "" })
// Фильтрация: price >= min && price <= max
```

#### 3. Особые фильтры
- ✅ Только новинки (`isNew`)
- ✅ Только хиты (`isBestseller`)

### 📊 Сортировка
```typescript
const sortOptions = [
  { id: "popular", name: "По популярности" },      // bestsellers → stock
  { id: "price-asc", name: "Цена: по возрастанию" },
  { id: "price-desc", name: "Цена: по убыванию" },
  { id: "name", name: "По названию" },            // localeCompare
  { id: "new", name: "Сначала новинки" },         // isNew first
]
```

### 👁️ Режимы отображения
- **Grid**: `grid-cols-1 sm:grid-cols-2 xl:grid-cols-3`
- **List**: Flex-верстка с горизонтальным расположением
- Переключатель с иконками
- Сохранение при переключении фильтров

---

## 🎨 UI/UX Решения

### 1. Счетчик активных фильтров
```typescript
const activeFiltersCount = [
  selectedCategory !== "all",
  priceRange.min || priceRange.max,
  showNewOnly,
  showBestsellersOnly
].filter(Boolean).length
// Показываем badge на кнопке "Фильтры"
```

### 2. Sticky header
```css
/* Фильтры прилипают при скролле */
position: sticky;
top: 32px; /* под основную навигацию */
```

### 3. Анимации
```typescript
// Framer Motion для плавного появления
<motion.div
  layout
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  exit={{ opacity: 0, y: -20 }}
>
```

### 4. Empty State
- Иконка поиска
- Текст "Товары не найдены"
- Кнопка "Сбросить фильтры"

### 5. Breadcrumbs
```typescript
<div className="flex items-center gap-2">
  <Link href="/">Главная</Link>
  <span>/</span>
  <span>Каталог</span>
</div>
```

---

## 📦 Структура данных

### Тип Product
```typescript
interface Product {
  id: number
  name: string              // Название
  price: number             // Цена в рублях
  weight: string            // Вес ("50 г")
  description: string       // Описание для поиска
  image: string             // URL изображения
  category: string          // Категория для фильтра
  isNew?: boolean           // Метка новинки
  isBestseller?: boolean    // Метка бестселлера
  stock: number             // Остаток на складе
}
```

### Категории
- `vegetables` — Овощи
- `herbs` — Зелень/травы
- `legumes` — Бобовые
- `seeds` — Семена

---

## ⚡ Оптимизации

### 1. useMemo для фильтрации
```typescript
const filteredProducts = useMemo(() => {
  // Все фильтрации здесь
  return result
}, [searchQuery, selectedCategory, sortBy, priceRange, showNewOnly, showBestsellersOnly])
```

### 2. Анимация списка
- `AnimatePresence` для плавного удаления/добавления
- `layout` prop для анимации изменения позиций

### 3. Ленивая загрузка изображений
```typescript
<Image
  src={product.image}
  loading="lazy"  // Для товаров ниже fold
  // priority для первых 3 товаров
/>
```

---

## 🎨 Стилизация (Tailwind)

### Цветовая схема
- Primary: `#16a34a` (green-600)
- Primary Light: `#dcfce7` (green-100)
- Background: `#fafaf9` (stone-50)

### Hover эффекты
```css
/* Карточка товара */
hover:shadow-xl hover:-translate-y-1 transition-all duration-300

/* Кнопки */
hover:bg-[#15803d] hover:shadow-lg

/* Фильтры */
hover:bg-[#dcfce7] text-[#16a34a]
```

### Отзывчивость
```css
/* Grid */
grid-cols-1 sm:grid-cols-2 xl:grid-cols-3

/* Sidebar */
hidden lg:block  /* На мобильных фильтры скрыты */
```

---

## 🔧 Интеграция с корзиной

### Локальное состояние количества
```typescript
const [quantity, setQuantity] = useState(0)

// Логика
quantity === 0 ? <Button onClick={handleAddToCart}>В корзину</Button>
               : <CounterButtons />
```

### Для реальной корзины заменить на:
```typescript
// Вариант 1: Context API
const { addToCart, updateQuantity, cart } = useCart()

// Вариант 2: Zustand
const quantity = useCartStore(state => state.getItemQuantity(product.id))

// Вариант 3: Redux
const quantity = useSelector(state => selectItemQuantity(state, product.id))
```

---

## 📝 Чеклист для нового каталога

### Перед началом:
- [ ] Определить структуру данных (Product interface)
- [ ] Подготовить массив товаров с категориями
- [ ] Решить: нужен ли поиск по описанию?
- [ ] Определить варианты сортировки

### Реализация:
- [ ] Создать `/app/catalog/page.tsx`
- [ ] Настроить состояния фильтров (useState)
- [ ] Реализовать useMemo для фильтрации
- [ ] Добавить компонент ProductCard
- [ ] Стилизовать фильтры (sidebar)
- [ ] Добавить toolbar (сортировка + view mode)
- [ ] Реализовать empty state
- [ ] Добавить breadcrumbs

### UX улучшения:
- [ ] Счетчик активных фильтров
- [ ] Кнопка "Сбросить фильтры"
- [ ] Sticky позиционирование
- [ ] Анимации (framer-motion)
- [ ] Hover эффекты на всех интерактивных элементах

### Оптимизация:
- [ ] useMemo для фильтрации
- [ ] AnimatePresence для списка
- [ ] Ленивая загрузка изображений
- [ ] Debounce для поиска (при >100 товаров)

---

## 🚀 Расширение функционала

### 1. Пагинация
```typescript
const ITEMS_PER_PAGE = 12
const [page, setPage] = useState(1)
const paginatedProducts = filteredProducts.slice(0, page * ITEMS_PER_PAGE)
```

### 2. URL-параметры
```typescript
// Сохранение фильтров в URL
const router = useRouter()
const searchParams = useSearchParams()

// При изменении фильтра
router.push(`/catalog?category=${category}&sort=${sortBy}`)
```

### 3. Бесконечная прокрутка
```typescript
// Intersection Observer
const { ref, inView } = useInView()
useEffect(() => {
  if (inView) setPage(p => p + 1)
}, [inView])
```

### 4. SSR для SEO
```typescript
// Для публичных каталогов
export async function generateMetadata() {
  return {
    title: "Каталог микрозелени | MicroGreen",
    description: "..."
  }
}
```

---

## ❌ Чего НЕ делать

1. **Не загружать все товары сразу** (при 1000+ товарах)
2. **Не делать поиск без debounce** (при частых запросах)
3. **Не использовать index как key** (при сортировке)
4. **Не забывать о мобильной версии** (фильтры в drawer/модалке)
5. **Не делать фильтрацию на сервере** (для <100 товаров)

---

## 📚 Полезные ссылки

- [Framer Motion Layout Animations](https://www.framer.com/motion/layout-animations/)
- [Next.js App Router](https://nextjs.org/docs/app)
- [Tailwind Grid](https://tailwindcss.com/docs/grid-template-columns)
- [useMemo Best Practices](https://react.dev/reference/react/useMemo)

---

## ✅ Текущий статус (MicroGreen Shop)

**Реализовано:**
- ✅ Главная: 3 популярных товара
- ✅ Страница каталога: 12 товаров
- ✅ Поиск по названию и описанию
- ✅ 5 вариантов сортировки
- ✅ Фильтры по категориям, цене, меткам
- ✅ Два режима отображения (Grid/List)
- ✅ Анимации с framer-motion
- ✅ Empty state
- ✅ Breadcrumbs
- ✅ Счетчик активных фильтров

**Дата создания:** 2026-02-16  
**Версия:** 1.0  
**Статус:** Production-ready

---

**💡 Запомни:** Эта структура каталога — универсальная и может быть использована в любом Next.js проекте с минимальными изменениями!
